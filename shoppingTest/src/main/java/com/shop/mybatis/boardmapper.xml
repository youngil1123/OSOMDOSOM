<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org/DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.shop.mapper.BoardMapper">

	<insert id="insert" parameterType="Board">
		
	</insert>

	<select id="select" parameterType="Integer" resultType="Board">
		
	</select>


	<update id="update">
		
	</update>

	<delete id="delete">
		
	</delete>
	
	<select id="selectall" resultType="Board">
		
	</select>
	<select id="searchmylist" resultType="Board" parameterType="String">
	select (#{board_no},#{review},#{writedate},#{star_rate}) from board where mem_id=#{mem_id}
	</select>
	
	

</mapper>